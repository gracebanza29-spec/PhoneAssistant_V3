<?xml version="1.0" encoding="utf-8"?>
<!--
    Fond de touche pavé numérique — style One UI RÉEL :
    Touches TRANSPARENTES (pas de fond visible).
    Seul le ripple apparaît à l'appui, confiné dans un masque ovale.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <!-- Masque ovale : confine le ripple dans la zone de la touche -->
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white"/>
        </shape>
    </item>
    <!-- Aucun fond permanent : touches transparentes comme One UI -->
</ripple>
